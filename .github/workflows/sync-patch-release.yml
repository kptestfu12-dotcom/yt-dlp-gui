name: Sync, Patch, and Release

on:
  workflow_dispatch:
    inputs:
      tag_suffix:
        description: 'Suffix for the tag (e.g. patch-1)'
        required: false
        default: 'patch-1'

permissions:
  contents: write

jobs:
  sync-and-patch:
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.get_version.outputs.TAG }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Sync and Patch
        run: |
          git config --global user.name "GitHub Action"
          git config --global user.email "action@github.com"
          
          # 1. Save our custom patch
          mkdir -p ../temp_files
          cp cli-args-support.patch ../temp_files/
          
          # 2. Add the original repository as a remote
          git remote add upstream https://github.com/imsyy/yt-dlp-gui.git
          git fetch upstream master
          
          # 3. Force reset our master to the original's master
          git checkout master
          git reset --hard upstream/master
          
          # 4. Restore and Apply the patch
          cp ../temp_files/cli-args-support.patch .
          git apply cli-args-support.patch
          
          # 5. Commit the changes
          git add .
          git commit -m "Auto-sync with upstream and apply custom patch"
          
          # 6. Push back to master
          git push origin master --force

      - name: Get Version
        id: get_version
        run: |
          # Use jq to parse version from tauri.conf.json
          VERSION=$(jq -r '.version' src-tauri/tauri.conf.json)
          echo "VERSION=$VERSION" >> $GITHUB_OUTPUT
          echo "TAG=v$VERSION-${{ github.event.inputs.tag_suffix }}" >> $GITHUB_OUTPUT

  build-and-release:
    needs: sync-and-patch
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: windows-latest
            target: x86_64-pc-windows-msvc
          - platform: macos-latest
            target: aarch64-apple-darwin
          - platform: ubuntu-22.04
            target: x86_64-unknown-linux-gnu

    runs-on: ${{ matrix.platform }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: master # Ensure we use the freshly pushed master

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install Linux dependencies
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install frontend dependencies
        run: pnpm install

      - name: Build and Release
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          tagName: ${{ needs.sync-and-patch.outputs.tag }}
          releaseName: "YDL GUI ${{ needs.sync-and-patch.outputs.tag }}"
          releaseDraft: false
          prerelease: false
          generateReleaseNotes: true
          includeUpdaterJson: true
          args: --target ${{ matrix.target }}
